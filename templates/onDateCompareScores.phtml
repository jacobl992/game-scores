<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8"/>
    <title>Scores by Date</title>
    <link rel="stylesheet" href="../css/homestyles.css">
    <script defer src="../js/filterDate.js"></script>
</head>

<body>
<p><a href="/">Home</a></p>
<h1>Scores Filtered to <span id="selected-date"></span></h1>
<p class="instructions">Currently, scores are only added to total if both players played the same game on the same day</p>
<p class="instructions">The below scores a filtered by date</p>
<h2>Low is good, high is bad</h2>
<h3>Overall scores:</h3>
<label for="date">Dates:</label>
<select class="date" name="date" required>
    <option value="">Select a date</option>
    <?php echo \App\ViewHelpers\ScoresHelper::datesDropdown($dateList) ?>
</select>
<button class="filter-button">Filter</button>
<button class="clear-filter">Clear Date Filter</button>
<div class="alert"></div>
<?php echo App\ViewHelpers\ScoresHelper::displayTotalScore($allScores) ?>
<h3>Scores by Game:</h3>
<table>
    <thead>
    <tr>
        <th>Game</th>
        <th class="center-in-t">C-dawg</th>
        <th class="center-in-t">J-dawg</th>
        <th class="center-in-t">Bestest</th>
    </tr>
    </thead>
    <tbody>
    <?php echo App\ViewHelpers\ScoresHelper::displayGameScores($allScores, $gameList) ?>
    </tbody>
</table>

</body>
</html>